// Wrapper to include all strings.

const MESSAGE = {
  paramsRequestRequired: 'The hostname, token and query parameters are required.',
  paramsQueryString: 'The query parameter must be a string.',
};

export default MESSAGE;